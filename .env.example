# Exemplo de variáveis de ambiente

# A aplicação atual não exige qualquer chave, pois utiliza Leaflet
# com tiles do OpenStreetMap.  Caso deseje usar a API do Google Maps,
# você poderá fornecer uma chave aqui e adaptar o código em
# `index.html` para carregar o script do Google.

# GOOGLE_MAPS_API_KEY=coloque_sua_chave_aqui